#!/usr/bin/env bash
# SPDX-License-Identifier: GPL-2.0-only

#
# LGTM coverage for CONFIG_LNET.
#

. "$(dirname "$(readlink -e "${BASH_SOURCE[0]}")")/lgtm.sh"

require-lgtm-kernel-config
require-kernel-config LNET=m

main "$@"
