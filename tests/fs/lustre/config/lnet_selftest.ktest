#!/usr/bin/env bash
# SPDX-License-Identifier: GPL-2.0

#
# LGTM coverage for CONFIG_LNET_SELFTEST.
#

. "$(dirname "$(readlink -e "${BASH_SOURCE[0]}")")/lgtm.sh"

require-lgtm-kernel-config
require-kernel-config LNET_SELFTEST=m

main "$@"
